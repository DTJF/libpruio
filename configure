#!/usr/bin/env bash

# Clean up cmake stuff
rm -rf CMakeCache.txt `find . -name CMakeFiles -print` Makefile
rm -rf debian/cmakefbc
rm -rf debian/cmakefbc.*
rm -rf debian/cmakefbc.*.*

# And run cmake
cmake -D CMAKE_INSTALL_PREFIX:PATH=/usr .
